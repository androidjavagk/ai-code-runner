// askAI.js
async function askAI(prompt) {
    // dummy placeholder — replace with actual OpenAI call later
    return `// sample response for prompt: ${prompt}`;
}

module.exports = { askAI }; // module export
